<% if @task.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-error">
      <%= pluralize(@task.errors.count, "error") %> prohibited this article from being saved:
    </div>
    <ul>
      <% @task.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>